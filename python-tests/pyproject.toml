[project]
name = "rust-py-models-python-tests"
version = "0.0.0"
requires-python = ">=3.10,<3.15"
dependencies = []

[dependency-groups]
dev = [
    "mypy==2.3.1",
    "ruff==0.15.12",
]

[tool.uv]
package = false
required-version = "==0.10.0"
